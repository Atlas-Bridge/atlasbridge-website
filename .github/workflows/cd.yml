name: CD

on:
  push:
    branches: [main]
    tags: ["v*"]

# Only one deploy at a time
concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  # Wait for CI to pass before deploying
  ci-check:
    name: Verify CI passed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run check

      - name: Unit tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Verify build artifacts
        run: |
          test -d dist/public && echo "Client build: OK"
          test -f dist/index.cjs && echo "Server build: OK"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: production-build-${{ github.sha }}
          path: dist/
          retention-days: 30

  deploy:
    name: Deploy
    needs: ci-check
    runs-on: ubuntu-latest
    # Prevent deployment from forks
    if: github.repository == 'Atlas-Bridge/atlasbridge-website'
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: production-build-${{ github.sha }}
          path: dist/

      - name: Verify artifact integrity
        run: |
          test -d dist/public && echo "Client assets: OK"
          test -f dist/index.cjs && echo "Server bundle: OK"

      # ──────────────────────────────────────────────
      # DEPLOY STEP — Replace with your hosting provider
      # ──────────────────────────────────────────────
      # Examples:
      #
      # Render:
      #   - name: Deploy to Render
      #     run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
      #
      # Railway:
      #   - name: Deploy to Railway
      #     uses: bervProject/railway-deploy@main
      #     with:
      #       railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #       service: atlasbridge-website
      #
      # Fly.io:
      #   - name: Deploy to Fly.io
      #     uses: superfly/flyctl-actions/setup-flyctl@master
      #   - run: flyctl deploy --remote-only
      #     env:
      #       FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      #
      # ──────────────────────────────────────────────
      - name: Deploy placeholder
        run: |
          echo "================================================"
          echo "  BUILD VERIFIED — READY FOR DEPLOYMENT"
          echo "  Commit: ${{ github.sha }}"
          echo "  Ref: ${{ github.ref }}"
          echo "================================================"
          echo ""
          echo "To enable deployment, replace this step with"
          echo "your hosting provider's deploy command."
          echo "See .github/workflows/cd.yml for examples."
